{
  "scripts": {
    "predeploy": "rm -rf public && mkdir public && cp dist/index.html dist/app.js dist/styles.css public/",
    "deploy": "wrangler pages deploy"
  },
  "dependencies": {
    "lunr": "^2.3.9"
  }
}
